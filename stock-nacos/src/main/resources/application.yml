server:
  port: 9001

spring:
  application:
    name: stock-nacos

  cloud:
    nacos:
      #nacos注册中心地址
      server-addr: localhost:8848
      discovery:
        username: nacos
        password: nacos
        namespace: public